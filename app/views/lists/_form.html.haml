= form_for list, remote: true do |f|
  - if list.errors[:name].any?
    %ul.error-list
      - list.errors[:name].each do |error|
        %li Name #{error}.
  = f.label :name
  = f.text_field :name, autofocus: true
  = f.submit
