$("li#list-<%= @list.id %>").remove();

if ($("ul#lists").children().length == 0) {
    $("#lists-container").
        replaceWith("<%=j render partial: 'lists',
                    locals: {lists: @lists}%>");
}
