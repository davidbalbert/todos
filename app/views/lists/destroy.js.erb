$("li#list-<%= @list.id %>").remove();

<% if @lists.count == 0 %>
    $("#lists-container").
        replaceWith("<%=j render partial: 'lists_container',
                    locals: {lists: @lists}%>");
<% end %>
